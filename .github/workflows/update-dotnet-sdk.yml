name: update-dotnet-sdk

on:
  schedule:
    - cron:  '0 10 * * WED'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  update-dotnet-sdk:
    uses: martincostello/update-dotnet-sdk/.github/workflows/update-dotnet-sdk.yml@13140139e28b28d1f5d8924472a297e30e020a10 # v6.1.1
    permissions:
      contents: write
      pull-requests: write
    with:
      labels: 'dependencies,.NET'
      update-nuget-packages: false
    secrets:
      repo-token: ${{ secrets.GITHUB_TOKEN }}

  add-security-label:
    needs: update-dotnet-sdk
    permissions:
      pull-requests: write
    runs-on: ubuntu-24.04
    if : |
      needs.update-dotnet-sdk.outputs.sdk-updated =='true' &&
      needs.update-dotnet-sdk.outputs.security == 'true'
    steps:
    - name: Add security label
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PR_URL: ${{ needs.update-dotnet-sdk.outputs.pull-request-html-url }}
      run: gh pr edit "${PR_URL}" --add-label security
